generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Project {
  id         String   @id @default(uuid())
  oldSiteUrl String
  newSiteUrl String
  sitemapUrl String? // Optional sitemap override
  createdAt  DateTime @default(now())

  @@map("projects")
}
