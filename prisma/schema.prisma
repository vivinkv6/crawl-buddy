generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Project {
  id         String   @id @default(uuid())
  oldSiteUrl String
  newSiteUrl String
  createdAt  DateTime @default(now())

  @@map("projects")
}
